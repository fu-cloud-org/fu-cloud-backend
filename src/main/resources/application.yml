server:
  port: 8000

web:
  domain: http://localhost

spring:
  thymeleaf:
    cache: false
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 123456
    url: jdbc:mysql://localhost:3306/fu-cloud?useSSL=true&useUnicode=true&characterEncoding=utf8
    type: com.zaxxer.hikari.HikariDataSource

logging:
  level:
    root: info
    com.fucloud.fucloudbackend.dao: debug

